import { Image } from '@nextui-org/react';
import './styles.scss';

interface musicProps {
  id: string;
  image: string;
}
interface cardSliderTrackProps {
  data: musicProps[];
}

export const CardSliderTrack = ({ data }: cardSliderTrackProps) => {
  return;
};
